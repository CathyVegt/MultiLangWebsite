/** 
 * start of config file
 * at the moment used to declare port 3000, such that the demo works on Glitch 
 * Glitch uses port 3000 as default 
 */
export default ({
    // ...some configs
    server: {
      port: 3000,
    },
  });